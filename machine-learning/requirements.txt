# Phase 1 (ML) dependencies.
# Install into the repo-root virtualenv:
#   uv venv --python 3.12 .venv
#   .venv/bin/python -m pip install -r machine-learning/requirements.txt
#
# torch/torchvision ship bundled CUDA (cu128 wheels on Linux) — no separate
# CUDA toolkit install is required for an NVIDIA GPU with a recent driver.
torch>=2.6
torchvision>=0.21
timm>=1.0.9
# facenet-pytorch is installed separately with --no-deps: its metadata pins
# torchvision<0.18, which conflicts with the CUDA-12 torch wheels above. The
# MTCNN code itself works fine on current torchvision. See README "Setup".
#   .venv/bin/python -m pip install --no-deps facenet-pytorch>=2.6
requests>=2.31          # runtime dependency of facenet-pytorch
opencv-python-headless>=4.8
scikit-learn>=1.3
matplotlib>=3.8
seaborn>=0.13
pandas>=2.0
numpy>=1.26
Pillow>=10.0
tqdm>=4.66
huggingface-hub[hf_transfer]>=0.26
